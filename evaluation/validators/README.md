# F1 — Deterministic and source-grounded validator mesh

Resolves everything checkable by schema, computation, retrieval, graph logic, or exact evidence before invoking a probabilistic judge.

Plane: F (Verification, hardening, escalation) · Phase: 5 — Verifier-trust proof (`BLOCKED`)

New subdirectory — harvey-labs' `evaluation/judge.py` has no dedicated deterministic-validator layer to fork (per the repository architecture).

## Build

- [ ] File/schema/version validators and document inventory/completeness checks
- [ ] Citation parse/existence/status and quotation-span match
- [ ] Jurisdiction/hierarchy/cutoff, clause/fact/defined-term extraction, amendment/order-of-precedence
- [ ] Cross-reference/signature, date/deadline, arithmetic/cap/basket/threshold validators
- [ ] Entity/ownership/cap-table reconciliation and cross-document consistency checks
- [ ] Evidence-span/required-output checks and mounted-document instruction separation

**Depends on:** B2/B4, C4, D3, E1/E2. Feeds F2–F5, G1/G3/G4.
**Posture:** Build legal validator orchestration/domain rules; integrate parsers, databases, standard math/schema libraries.
**Done when:** per-validator gold accuracy is measured; FP/FN confidence intervals exist by severity/family; validators fail closed or abstain rather than guess.
**Avoid:** a parser error mislabeled as a model error; existence-only citation verification; free-form extraction masquerading as deterministic.

## AR experiment

**Run AR-003 here:** severe false-accept upper bound must clear the pre-declared threshold on the paired gold/attack corpus.
