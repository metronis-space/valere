# registry/ — Plane G (part 2): Artifact registry, release, refresh

New directory with no harvey-labs equivalent. Owns the one component, [g5-artifact-registry-release-refresh/](g5-artifact-registry-release-refresh/). Status: `BLOCKED`.

**Compute constraint:** the deployment target is Modal (`modal deploy`), not Kubernetes/Baseten/SageMaker endpoints.
